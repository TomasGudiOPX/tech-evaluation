# OpenSpec

This directory holds durable project specifications and active change proposals.

- `specs/` contains accepted behavior and operational decisions.
- `changes/` is reserved for proposed work that has not been implemented yet.

Keep specifications short and observable. Update a specification when a template change alters deployment behavior, public interfaces, security boundaries, or the development workflow.
