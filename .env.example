COMPOSE_PROJECT_NAME=vps-template
HTTP_PORT=8080

POSTGRES_DB=app
POSTGRES_USER=app
POSTGRES_PASSWORD=replace-this-before-deploying

# Needed only when the frontend is served separately during development.
CORS_ORIGIN=http://localhost:5173
